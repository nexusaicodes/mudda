# Active Storage services. Local disk only — development, test, and production
# all store uploads on the file system under storage/ (or tmp/ for tests).
test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage/files") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage", Rails.env, "files") %>
