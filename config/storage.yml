<%
  config_path = if Mudda.saas?
    gem_path = Gem::Specification.find_by_name("mudda-saas").gem_dir
    File.join(gem_path, "config", "storage.yml")
  else
    File.join(__dir__, "storage.oss.yml")
  end
%>
<%= ERB.new(File.read(config_path)).result %>
