<%= turbo_frame_tag @note, :container do %>
  <div id="<%= dom_id(@note) %>" class="note flex align-start full-width">
    <figure class="note__avatar flex-item-no-shrink" aria-hidden="true">
      <%= avatar_tag @note.creator, hidden_for_screen_reader: true %>
    </figure>

    <div class="note__content flex flex-column flex-item-grow full-width">
      <div class="note__body" data-turbo-permanent>
        <%= form_with model: [ @card.board, @card, @note ], class: "flex flex-column gap full-width",
              data: { controller: "form", action: "keydown.ctrl+enter->form#submit:prevent keydown.meta+enter->form#submit:prevent keydown.esc->form#cancel:stop" } do |form| %>
          <%= form.rich_textarea :body, required: true, autofocus: true, placeholder: new_note_placeholder(@card) do %>
            <%= general_prompts(@card.board) %>
          <% end %>
          <div class="flex gap-half justify-start align-center">
            <%= form.button class: "btn btn--reversed", type: :submit, title: "Save changes (#{ hotkey_label(["ctrl", "enter"]) })" do %>
              <span>Save</span>
            <% end %>
            <%= link_to board_card_note_path(@card.board, @card, @note), class: "btn", data: { form_target: "cancel" },title: "Cancel (#{ hotkey_label(["esc"]) })" do %>
              <span>Cancel</span>
            <% end %>

            <%= tag.button type: :submit, class: "btn btn--negative flex-item-justify-end", form: dom_id(@note, :delete_form),
                  data: { turbo_confirm: "Are you sure you want to delete this note?" } do %>
              <%= icon_tag "trash" %>
              <span class="for-screen-reader">Delete</span>
            <% end %>
          </div>
        <% end %>

        <%= form_with url: board_card_note_path(@card.board, @card, @note), method: :delete, id: dom_id(@note, :delete_form), data: { turbo_frame: "_top" } %>
      </div>
    </div>
  </div>
<% end %>
