<% if card.due_on.present? %>
  <span class="card__due card__due--<%= card_due_urgency(card) %>" title="Due <%= l(card.due_on, format: :long) %>">
    <%= icon_tag "bell-alert" if card.overdue? %>
    Due <%= card_due_label(card) %>
  </span>
<% end %>
