<lexxy-prompt-item>
  <template type="menu">
    #<%= card.number %> <%= card.title %>
  </template>
  <template type="editor">
    <%= link_to "##{card.number} #{card.title}", board_card_path(card.board, card) %>
  </template>
</lexxy-prompt-item>
