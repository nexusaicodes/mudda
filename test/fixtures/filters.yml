newest_first:
  id: <%= ActiveRecord::FixtureSet.identify("newest_first", :uuid) %>
  creator: david_uuid
  fields: <%= { indexed_by: :all, sorted_by: :newest }.to_json %>
  params_digest: <%= Filter.digest_params({ indexed_by: :all, sorted_by: :newest }) %>
  account: 37s_uuid
